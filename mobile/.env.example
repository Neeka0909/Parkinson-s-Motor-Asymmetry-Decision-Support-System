# Use your PC's LAN IP — NOT localhost (phones cannot reach localhost on your PC)
# Find IP: ipconfig → IPv4 Address on your Wi-Fi adapter
# Example (replace with your IP):
EXPO_PUBLIC_API_URL=http://192.168.100.243:5000/api/v1
